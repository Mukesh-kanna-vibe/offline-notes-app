name: offline_notes
description: "Offline-first notes app with sync and conflict resolution."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  sqflite: ^2.4.2
  path: ^1.9.1
  connectivity_plus: ^6.1.4
  dio: ^5.8.0+1
  uuid: ^4.5.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
