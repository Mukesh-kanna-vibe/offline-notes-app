{
  "name": "offline-notes-mock-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0 --delay 500"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
